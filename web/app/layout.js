import './globals.css'

export const metadata = { title: 'Ideas Hub', description: 'Vietnam Opportunity Intelligence' }

export default function RootLayout({ children }) {
  return <html lang="vi"><body>{children}</body></html>
}
